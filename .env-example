# Example Environment Variables for the Drift Trading Bot

# Copy this file to .env and fill in your actual values.

# Do NOT commit your actual .env file to Git.

# --- Wallet Configuration ---

# Path to the Solana keypair file (JSON format)

# Generate this using `solana-keygen new --outfile my-keypair.json` or `solana-keygen grind`

SOLANA_KEYPAIR_PATH=path/to/your/solana-keypair.json

# --- Environment Configuration ---

# Set to 'devnet' for testing or 'mainnet' for live trading

DRIFT_ENV=devnet

# --- Solana RPC Endpoints ---

# You can use public endpoints or your own private RPC provider (e.g., Helius, QuickNode)

# The defaults in config.ts (api.devnet.solana.com / api.mainnet-beta.solana.com) are used if these are commented out.

# DEVNET_RPC_URL=https://api.devnet.solana.com

# MAINNET_RPC_URL=https://api.mainnet-beta.solana.com

# Optional: Uncomment and set if you want to override the default RPC URLs

# DEVNET_RPC_URL=

# MAINNET_RPC_URL=

# --- Solana WebSocket Endpoints (Optional) ---

# Only needed if you require WebSocket subscriptions for real-time updates.

# Often the same domain as RPC but with wss://

# DEVNET_WS_URL=

# MAINNET_WS_URL=
