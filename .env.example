# .env-example
# Example configuration file. Copy this to .env and fill in your actual values.
# NEVER COMMIT YOUR ACTUAL .env FILE TO GIT!

# General Settings
NODE_ENV=development # 'development' or 'production'

# Drift 
DRIFT_ENV=mainnet

# Wallet Configuration
# Path relative to the project root (e.g., trading_bot_v1)
SOLANA_KEYPAIR_PATH=secrets/your-keypair-file.json
EXPECTED_PUBLIC_KEY=YOUR_EXPECTED_BOT_PUBLIC_KEY # Public key corresponding to the keypair file

# --- Devnet Settings ---
# (Optional) Use your own Devnet RPC URL, otherwise defaults to public Solana RPC
DEVNET_RPC_URL=
# (Optional) WebSocket URL for Devnet
DEVNET_WS_URL=
# (Optional) Solana Commitment level for Devnet transactions (e.g., processed, confirmed, finalized)
DEVNET_SOLANA_COMMITMENT=confirmed
# Drift environment for Devnet
DEVNET_DRIFT_ENV=devnet

# --- Mainnet Settings ---
# IMPORTANT: Use Mainnet with extreme caution!
# (Optional) Use your own Mainnet RPC URL, otherwise defaults to public Solana RPC
MAINNET_RPC_URL=
# (Optional) WebSocket URL for Mainnet
MAINNET_WS_URL=
# (Optional) Solana Commitment level for Mainnet transactions
MAINNET_SOLANA_COMMITMENT=confirmed
# Drift environment for Mainnet
MAINNET_DRIFT_ENV=mainnet-beta 